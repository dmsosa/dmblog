export type TUser = {
    id: number | null,
    username: string | null,
    email: string | null,
    password: string | null,
    image: string | null,
    bio: string | null,
    followersCount: number | null, 
    followingCount: number | null,
}